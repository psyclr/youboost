# Claude Code Hooks - MCP Integrations

Эта директория содержит автоматические hooks для интеграции Claude Code с внешними сервисами через MCP (Model Context Protocol).

## 🎯 Доступные Hooks

### 1. `on-task-start`

**Триггер**: Когда Claude начинает новую задачу

**Функции**:

- 📋 Создание задачи в Linear
- 🌿 Создание feature branch в Git
- 📝 Обновление .claude.md с текущим фокусом

**MCP интеграции**:

- **Linear**: Создание issue в Linear для трекинга задачи

### 2. `on-task-complete`

**Триггер**: Когда Claude завершает задачу

**Функции**:

- ✅ Запуск финальных проверок (validate)
- 📝 Обновление истории изменений в .claude.md
- ✓ Обновление статуса задачи в Linear
- 📊 Опционально: создание GitHub issue с резюме

**MCP интеграции**:

- **Linear**: Обновление статуса задачи на "Done"
- **GitHub**: Создание issue с резюме выполненной работы

### 3. `on-error`

**Триггер**: Когда Claude сталкивается с ошибкой

**Функции**:

- 📝 Логирование ошибки в `.claude/error.log`
- 💾 Сохранение контекста ошибки
- 🐛 Создание GitHub issue для критических ошибок

**MCP интеграции**:

- **GitHub**: Автоматическое создание bug issue с полным контекстом

### 4. `on-plan-create`

**Триггер**: Когда Claude создает план в plan mode

**Функции**:

- ✅ Валидация структуры плана
- 💾 Сохранение копии в `docs/plans/`
- 📝 Обновление .claude.md со ссылкой на план
- 📋 Создание GitHub/Linear issue с планом

**MCP интеграции**:

- **GitHub**: Создание issue с полным текстом плана
- **Linear**: Создание epic/issue для реализации плана

### 5. `on-file-edit`

**Триггер**: Когда Claude редактирует файл

**Функции**:

- 🔍 Запуск ESLint с автофиксом
- ✨ Автоформатирование через Prettier
- 🔎 Type checking для TypeScript
- 📚 Получение документации для импортированных библиотек

**MCP интеграции**:

- **Context7**: Автоматическое получение документации для новых импортов

### 6. `on-review-request`

**Триггер**: Когда запрашивается code review

**Функции**:

- 📝 Static analysis (ESLint, TypeScript)
- 🧪 Проверка наличия тестов
- 🔒 Security audit
- 📊 Complexity analysis
- 📝 Создание PR review comment

**MCP интеграции**:

- **GitHub**: Автоматический комментарий в PR с результатами review

### 7. `on-tool-use`

**Триггер**: При использовании критических инструментов

**Функции**:

- 📦 Security audit при изменении зависимостей
- 📄 Проверка .gitignore при создании файлов
- 💾 Trigger post-commit hooks

## 🔧 Настройка MCP Интеграций

### Linear Integration

1. Получите Team ID из Linear:

   ```bash
   # В Linear: Settings → API → Teams
   LINEAR_TEAM_ID="your-team-id"
   ```

2. Раскомментируйте в `on-task-start`:

   ```bash
   LINEAR_TEAM_ID="your-team-id"
   claude mcp linear create-issue --team-id "$LINEAR_TEAM_ID" --title "$TASK_DESCRIPTION" --state "In Progress"
   ```

3. Раскомментируйте в `on-task-complete` для обновления статуса

### GitHub Integration

1. Получите имя репозитория (owner/repo):

   ```bash
   GITHUB_REPO="yourusername/youboost"
   ```

2. Раскомментируйте нужные интеграции в:
   - `on-task-complete`: создание issue при завершении
   - `on-error`: автоматические bug reports
   - `on-plan-create`: создание issue с планом
   - `on-review-request`: PR review comments

### Context7 Integration

Context7 работает автоматически для получения документации.

Раскомментируйте в `on-file-edit` для автоматического получения документации при импорте новых библиотек:

```bash
# Извлечь импорты и получить документацию
claude mcp context7 query-docs --library "$lib" --query "Getting started guide"
```

## 📋 Примеры использования

### Создание задачи с Linear

```bash
# Hook автоматически сработает при начале задачи
# Claude создаст issue в Linear с названием задачи
```

### Автоматический bug report в GitHub

```bash
# При возникновении ошибки hook автоматически создаст issue:
# Заголовок: "🐛 Error: {error_message}"
# Содержание: полный контекст ошибки + timestamp
# Labels: bug, automated
```

### Сохранение планов в GitHub

```bash
# После создания плана в plan mode:
# 1. План сохраняется в docs/plans/
# 2. Создается GitHub issue с полным текстом
# 3. Ссылка добавляется в .claude.md
```

## 🛠️ Разработка собственных hooks

### Структура hook файла

```bash
#!/bin/bash
# Описание что делает hook

PARAM1=$1
PARAM2=$2

echo "Hook triggered with: $PARAM1"

# Ваша логика

# MCP интеграция (опционально)
# claude mcp service-name action --param1 value1
```

### Доступные MCP сервисы

Установленные у вас:

- **Context7**: Документация и примеры кода
- **Figma**: Работа с Figma designs
- **GitHub**: Issues, PRs, projects, code
- **Linear**: Project management
- **Playwright**: Browser automation

### Использование MCP в hooks

```bash
# Linear
claude mcp linear create-issue --team-id "TEAM_ID" --title "Title" --state "In Progress"
claude mcp linear update-issue --issue-id "ISSUE_ID" --state "Done"

# GitHub
claude mcp github create-issue --repo "owner/repo" --title "Title" --body "Body" --labels "bug,enhancement"
claude mcp github create-pr-review-comment --repo "owner/repo" --pr "123" --body "Comment"

# Context7
claude mcp context7 query-docs --library "react" --query "hooks documentation"
claude mcp context7 resolve-library-id --library "express"
```

## 🔒 Безопасность

- Все hooks запускаются локально на вашей машине
- API ключи берутся из Claude Code MCP конфигурации
- Никакие секреты не хранятся в hooks файлах
- Все внешние вызовы логируются

## 📝 Логирование

Логи hooks сохраняются в:

- `.claude/error.log` - Лог ошибок
- `.claude/errors/` - Детальные контексты ошибок
- `docs/plans/` - Сохраненные планы

## 🐛 Troubleshooting

### Hook не запускается

```bash
# Проверьте права на выполнение
chmod +x .claude/hooks/*
```

### MCP команда не работает

```bash
# Проверьте что MCP сервис установлен
# В Claude Code: Settings → MCP Servers

# Тестируйте команду вручную
claude mcp linear --help
```

### Ошибка "command not found"

```bash
# Убедитесь что команда доступна в PATH
which npx
which git
which jq  # Для complexity analysis
```

## 📚 Дополнительные ресурсы

- [Claude Code Documentation](https://docs.anthropic.com/claude-code)
- [MCP Protocol](https://modelcontextprotocol.io)
- [Linear API](https://developers.linear.app)
- [GitHub API](https://docs.github.com/en/rest)
- [Context7](https://context7.com/docs)
